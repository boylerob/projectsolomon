const express = require('express');
const router = express.Router();

// Placeholder biblical routes
router.get('/', (req, res) => {
  res.json({ message: 'Biblical routes placeholder' });
});

module.exports = router; 